<nav class="navbar navbar-default navbar-fixed-top gradientheader">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <%=link_to( image_tag("purpleLogo.png", :class => "small_logo"), '/index') %>
            



            <% if current_user %>
                Signed in as <%= current_user.email %> | <%= link_to "Logout", '/logout' %>
            <% end %>
            </div>
            
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <% if current_user %>
                <ul class="nav navbar-nav navbar-right nav_options">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "Scoreboard", '/index' %>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "My Profile", current_user %>
                    </li>
                    <%if current_user.admin %>
                    <li class="page-scroll">
                        <%= link_to "Edit Activities", '/activities' %>
                    </li>
                    <% else %>
                    <!-- the user is a regular user, so link to add points TODO change link -->
                    <li class="page-scroll">
                        <%= link_to "Add Points", '/activities' %>
                    </li>
                    <%end%>
                </ul>
                <% else %>
                <ul class="nav navbar-nav navbar-right nav_options">
                    <li>
                        <%= link_to 'Login', '/login' %>
                    </li>
                    <li>
                        <%= link_to 'Signup', '/signup' %>
                    </li>
                </ul>
            <% end %>
            </div>
            
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
